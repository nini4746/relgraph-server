# RelGraph Server

## 개요

RelGraph Server는 사용자 행동 로그를 기반으로  
**아이템 간 연관 관계 그래프(Relation Graph)**를 구성하고,  
이를 이용해 **검색 및 추천 결과를 제공하는 서버**를 구현하는 프로젝트이다.

이 프로젝트의 목적은 대규모 서비스의 외형을 흉내 내는 것이 아니라,  
검색·추천 시스템의 핵심 원리인 **그래프 모델링, 색인, 탐색, 가중치 설계**를  
직접 구현하고 설명 가능한 형태로 만드는 데 있다.

머신러닝, 외부 추천 라이브러리, 상용 검색 엔진은 사용하지 않는다.

---

## 목표

- 사용자 이벤트로부터 **연관도 그래프를 구성**
- 그래프 기반 추천 결과 제공
- 색인 기반 검색 구조 직접 구현
- 데이터 규모가 커질 때 발생하는 **성능 문제를 체감**
- 설계 선택에 대한 명확한 설명 가능

---

## 핵심 개념

### 1. Relation Graph

- Node: ITEM, QUERY, USER
- Edge: 관계(예: WATCHED_AFTER, CO_CLICKED, QUERY_MATCH)
- Edge는 가중치를 가진다
- 가중치는 시간에 따라 감소할 수 있다

---

### 2. 이벤트 기반 데이터 모델

시스템은 상태를 직접 저장하지 않고,  
**이벤트(Event)**를 통해 관계를 강화한다.

예시 이벤트:
```json
{
  "userId": "u1",
  "type": "WATCH",
  "itemId": "v123",
  "timestamp": 1700000000
}
```

---

## 필수 기능 (Mandatory)

### 1. 이벤트 수집 API

- 사용자 행동 이벤트를 서버로 전송할 수 있어야 한다
- 이벤트 타입은 최소 3종 이상 정의할 것
  - 예: CLICK, WATCH, SEARCH

---

### 2. 연관도 그래프 구성

- 이벤트 발생 시 그래프를 갱신해야 한다
- 동일 관계는 누적 가중치로 관리한다
- 노드 간 관계는 **단방향/양방향 여부를 명확히 정의**해야 한다

---

### 3. 추천 API

- 특정 아이템 또는 사용자 기준으로 추천 결과를 반환한다
- 추천은 **그래프 탐색 결과**여야 한다
- 단순 인기순 정렬은 허용되지 않는다

---

### 4. 검색 기능

- 문자열 기반 검색을 지원해야 한다
- 색인 구조를 직접 설계·구현할 것
- 전체 순회(O(N)) 방식은 금지

---

### 5. 데이터 규모

아래 조건을 만족하는 데이터에서 정상 동작해야 한다.

- 아이템: **최소 10,000개**
- 이벤트 로그: **최소 500,000건**
- 평균 응답 시간은 합리적인 수준을 유지해야 한다

---

## 제한 사항

- 머신러닝 사용 금지
- 외부 검색 엔진 사용 금지 (Elasticsearch 등)
- Neo4j 등 전용 그래프 DB 사용 금지
- 추천 결과는 **설명 가능해야 한다**

---

## Bonus (선택)

- 시간 감쇠(weight decay) 적용
- 세션 기반 관계 강화
- 랜덤 워크 기반 추천
- 서브그래프 조회 API
- 캐싱 전략 도입

---

## 평가 기준

다음 항목을 중점적으로 평가한다.

1. 그래프 모델링의 타당성
2. 색인 구조 설계의 합리성
3. 데이터 규모 증가에 따른 성능 대응
4. 추천 결과의 일관성과 설명 가능성
5. 코드 구조의 명확성

---

## 제출물

- Git Repository
- 실행 방법 문서
- 데이터 생성 방식 설명
- 추천/검색 알고리즘 설명 문서

---

## 주의

이 프로젝트는 “유튜브를 흉내 내는 것”이 목적이 아니다.  
**추천이 왜 발생하는지 설명할 수 없는 결과는 감점 대상**이다.
